class Constants {
  static final String authTokenStoreKey = 'auth_token';
  static final String authType = 'Bearer';  
  static const Duration requestTimeoutMax = Duration(seconds: 15);
  static const Duration requestTimeoutMin = Duration(seconds: 5);
  static final String normal = 'normal';  
}